package com.linkedlist;

public class Node{
		Object ele;
		private Node next;
		public Node(Object e, Node n) {
			ele = e;
			next = n;
		}
	}
/*it is used for to implement arraylist
 * so we use node 
 * node  is also consider as an object 
 * it contains element and address(next)
 */

